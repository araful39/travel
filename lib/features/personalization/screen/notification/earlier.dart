import 'package:flutter/material.dart';

class Earlier extends StatelessWidget {
  const Earlier({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("This is Earlier"),
      ),
    );
  }
}
