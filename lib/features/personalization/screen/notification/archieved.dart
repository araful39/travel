import 'package:flutter/material.dart';

class Archieved extends StatelessWidget {
  const Archieved({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("This is Archieved"),
      ),
    );
  }
}
